'use client'
import React from "react"

export default class Slider extends React.Component {
    constructor(props){
        super(props)
    }

    render(){
        return(
            <section>

            </section>
        )
    }
}